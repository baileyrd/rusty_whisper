[package]
name = "rusty-whisper"
version = "0.1.0"
edition = "2021"
description = "A pure-Rust port of whisper.cpp (OpenAI Whisper speech recognition)"
license = "MIT"

[dependencies]

[profile.release]
lto = true
codegen-units = 1
